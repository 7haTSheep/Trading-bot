export function sendTradeNotification(message: string): void {
    // Logic to send trade notifications (e.g., via email, SMS, or app notification)
    console.log(`Trade Notification: ${message}`);
}

export function logTradeActivity(activity: string): void {
    // Logic to log trade activities (e.g., to a file or database)
    console.log(`Trade Activity Log: ${activity}`);
}